# DocumentMerge Platform — Описание проекта

## Стек технологий

• **Язык программирования:** Python 3.10+ с поддержкой асинхронного программирования (asyncio, async/await)  
• **Web-фреймворк:** FastAPI 0.109.0+ для создания REST API с автоматической OpenAPI документацией  
• **ASGI-сервер:** Uvicorn с поддержкой WebSocket и горячей перезагрузки для разработки  
• **База данных:** SQLAlchemy 2.0+ ORM с SQLite (возможность миграции на PostgreSQL)  
• **Валидация данных:** Pydantic 2.0+ для строгой типизации и валидации моделей запросов/ответов  
• **Аутентификация:** JWT-токены через python-jose с криптографией, хеширование паролей через passlib[bcrypt]  
• **Обработка документов:** PyMuPDF (fitz) 1.23.0+ для работы с PDF, python-docx 1.1.0+ для DOCX-файлов  
• **LLM-инфраструктура:** vLLM для высокопроизводительного инференса локальной модели OpenAI GPT-OSS-20B (10.109.50.250:1212)  
• **OCR-система:** Chandra Vision для распознавания текста из PDF-сканов с поддержкой 90+ языков и GPU-ускорения через PyTorch (10.109.50.250:8880)  
• **HTTP-клиент:** httpx 0.26.0+ для асинхронных запросов к ML-сервисам с поддержкой таймаутов  
• **Алгоритмы сравнения:** difflib (Python stdlib) для построчного diff, Longest Common Subsequence (LCS) для alignment, regex для извлечения финансовых параметров  
• **Обработка форм:** python-multipart для загрузки файлов через multipart/form-data  
• **Frontend:** Vanilla HTML5, CSS3 (custom design system), JavaScript ES6+ с Fetch API для асинхронных запросов  
• **Контейнеризация:** Docker с multi-stage builds, Docker Compose для оркестрации сервисов  
• **Логирование:** Python logging с ротацией файлов (max 10MB, 5 backup files), структурированные логи с уровнями DEBUG/INFO/WARNING/ERROR/CRITICAL  
• **Конфигурация:** Pydantic Settings с поддержкой .env файлов для управления переменными окружения

---

## Принцип работы

**DocumentMerge Platform** — это enterprise-платформа для интеллектуального анализа и сравнения документов (контрактов, договоров) с использованием AI. Система предоставляет **6 режимов сравнения**, автоматическое **извлечение сущностей** и **анализ рисков**.

### 1. Загрузка и обработка документов
Пользователь загружает PDF/DOCX/TXT через веб-интерфейс → FastAPI принимает файл через `/api/v1/documents/upload` → система сохраняет файл в директорию `uploads/` → в зависимости от формата:
- **PDF:** PyMuPDF извлекает текст постранично с сохранением структуры
- **DOCX:** python-docx парсит параграфы и таблицы
- **Сканы:** отправка на Chandra Vision OCR для распознавания текста

Метаданные (имя файла, размер, хэш MD5, пользователь, дата) и извлеченный текст сохраняются в SQLite базу через SQLAlchemy ORM.

### 2. Сравнение документов (Multi-Mode Diff Engine)
Система предлагает 6 режимов сравнения:

**Построчное (Line-by-Line):** разбивает документы на строки → использует `difflib.SequenceMatcher` для поиска операций (`equal`, `replace`, `delete`, `insert`) → для измененных строк выполняет word-level diff → выводит side-by-side таблицу с HTML-подсветкой и статистикой (+N/-M строк).

**Семантическое (Semantic Diff):** классифицирует изменения по типам (`FINANCIAL` — суммы/платежи, `TEMPORAL` — даты/сроки, `LEGAL` — ответственность/штрафы) → для числовых изменений рассчитывает процент отклонения → присваивает severity (`CRITICAL` >10%, `MAJOR` финансовые, `MINOR` текстовые) → генерирует AI-summary для каждого изменения с иконками (💰, 📅, ⚖️).

**Анализ влияния (Impact Analysis):** regex-extraction чисел с контекстом (суммы, сроки, проценты) → сравнивает параметры между версиями → рассчитывает изменение в процентах → threshold-based severity (>20% CRITICAL, 5-20% MAJOR) → выводит таблицу: Параметр | Было | Стало | Изменение (%) | Значимость.

**По пунктам (Clause-by-Clause):** парсит документ на структурные блоки (нумерованные разделы, именованные клаузулы) → сопоставляет клаузулы между версиями → определяет статус (ADDED/REMOVED/MODIFIED) → рассчитывает similarity score → LLM генерирует summary для каждого изменения.

**Юридический (Legal Diff):** keyword-based classification ("ответственност" → LIABILITY CRITICAL, "штраф" → PENALTY CRITICAL) → извлекает контекст (±50 символов) → определяет risk level → генерирует рекомендации (требуется ли юридическая проверка) → выводит risk matrix.

**Timeline-визуализация:** загружает все версии из БД → для каждой версии сравнивает с предыдущей + подсчитывает изменения → строит вертикальный timeline с узлами (цвет = risk level 🔴/🟡/🟢, толщина линии = количество изменений) → интерактивные функции (click → детальный diff, hover → preview).

### 3. Извлечение сущностей (Entity Extraction)
**LLM-based extraction (primary):** формирует промпт с текстом договора (первые 6000 символов) → отправляет на GPT-OSS-20B через vLLM → запрашивает извлечение в JSON: стороны, даты, платежные условия, штрафы, обязательства, условия расторжения, ответственность, конфиденциальность, применимое право, разрешение споров → парсит JSON-ответ.

**Regex-based extraction (fallback):** при недоступности LLM использует паттерны: `(?:ООО|ОАО)\s*[«"]([^»"]+)[»"]` для организаций, `ИНН[:\s]*(\d{10,12})` для ИНН, `(\d[\d\s]*[.,]?\d*)\s*(рубл|руб|₽)` для сумм, `(?:оплата|платеж)[^\d]*(\d+)\s*дн` для сроков, `(?:штраф)[^\d]*(\d+[.,]?\d*)\s*%` для штрафов → ищет значения в контексте ±500 символов от ключевых слов.

### 4. Автоматическое слияние (Merge Engine)
**Three-Way Merge:** определяет base version (общий предок) → для каждого блока текста: если изменен только в A → применяет A, если только в B → применяет B, если в обеих → CONFLICT → формирует merged документ с conflict markers.

**Multi-Way Merge (N версий):** стратегия `CONSENSUS` — для каждого блока подсчитывает идентичные варианты, если один встречается в >50% версий → применяется автоматически, иначе → conflict → пользователь выбирает через wizard-like UI с preview результата после каждого шага.

### 5. Анализ рисков (Risk Analysis)
Многомерный анализ выявляет критические изменения:

**Финансовые риски:** сумма >500k RUB, платежные условия >60 дней, штрафы >5% контракта, дисконты >20%.  
**Временные риски:** сроки <30 дней, пени >1% в день, автопродление без opt-out, уведомление о расторжении <30 дней.  
**Юридические риски:** отсутствие ограничения ответственности (CRITICAL), liability cap > contract value, отсутствие альтернативы арбитражу, иностранное право, нечеткое владение IP.

Каждый риск оценивается по шкале 0-100 → overall risk score = weighted average → классификация: 🟢 GREEN (0-30), 🟡 YELLOW (31-70), 🔴 RED (71-100) → выводит dashboard с breakdown по категориям и trend analysis (↑/↓ относительно предыдущих версий).

### 6. Аутентификация и аудит
**Регистрация:** email + пароль → хеширование через bcrypt (12 rounds) → сохранение в БД.  
**Авторизация:** POST `/api/v1/auth/login` → проверка пароля → генерация JWT access token (HS256, 24h validity) → токен содержит user_id, email, role.  
**Доступ:** Authorization header `Bearer <token>` → middleware декодирует токен → проверяет права.  
**Audit Trail:** каждое действие записывается в `audit_logs` (user_id, action, resource_type, timestamp) → логи сохраняются в `logs/app.log` с ротацией.

---

**Разработчик:** НИР-Центр, УГМК  
**Версия:** 2.0  
**Статус:** Production Ready  
**Дата:** 02.02.2026
